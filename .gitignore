# Tool / harness state (never commit)
.agents/
.workbuddy/

# Node
node_modules/

# 本项目只用 npm；混进来的 pnpm / yarn lockfile 一律不提交
pnpm-lock.yaml
yarn.lock

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.sw?

# Logs
logs
*.log

# 独立自动化浏览器（playwright-cli x-te）：登录态与本地快照，永不提交
browser-profiles/
.playwright-cli/

# 登录态导出文件（x.com 会话等，含凭据，任何位置/变体名都禁止提交）
auth*.json
cookie*.json
